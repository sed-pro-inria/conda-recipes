--- BUILD.txt.orig	2015-07-14 08:42:54.060065426 +0200
+++ BUILD.txt	2015-07-14 08:38:15.235877555 +0200
@@ -23,10 +23,12 @@
   prefix=[PATH]   - Set the installation prefix [/usr/local/ by default]
 
 Advanced debugging related options:
-  gdb=1       - Build with support for GDB [off by default]
-  debug=1     - Enable debugging support [off by default]
-  assert=1    - Enable asserts [off by default]
-  assert2=1   - Enable very expensive asserts [off by default]
+  gdb=1        - Build with support for GDB [off by default]
+  debug=1      - Enable debugging support [off by default]
+  assert=1     - Enable asserts [off by default]
+  assert2=1    - Enable very expensive asserts [off by default]
+  use_gkrand=1 - Use Gklib pseudo-random numbers, consistent across 
+                 platforms [off by default]
 
 METIS' index and real data type size can be configured by editing
 include/metis.h.
--- Makefile.orig	2015-07-14 08:42:35.667261101 +0200
+++ Makefile	2015-07-14 08:38:15.235877555 +0200
@@ -9,6 +9,7 @@
 gklib_path = not-set
 shared     = not-set
 cc         = not-set
+use_gkrand = not-set
 
 
 # Basically proxies everything to the builddir cmake.
@@ -51,6 +52,9 @@
 ifneq ($(cc), not-set)
     CONFIG_FLAGS += -DCMAKE_C_COMPILER=$(cc)
 endif
+ifneq ($(gkrand), not-set)
+    CONFIG_FLAGS += -DGKRAND=$(gkrand)
+endif
 
 VERNUM=5.1.0
 PKGNAME=metis-$(VERNUM)
