Documenting
===================================================================

Install `graphviz` from `apt-get` or other.

Install sphinx:

.. code-block:: bash

    conda install python=3.5 sphinx
